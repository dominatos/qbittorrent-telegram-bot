<?php

declare(strict_types=1);

return [
    // Telegram Bot Token (Get it from @BotFather)
    'bot_token' => 'XXXX:XXXXX',

    // List of Telegram, User IDs allowed to interact with the bot
    'allowed_user_ids' => [
        12345678,
        // 87654321
    ],

    // File to store bot state (relative to script or absolute path)
    'state_file' => __DIR__ . '/bot_state.json',

    // Log file path
    'log_file' => __DIR__ . '/bot.log',

    // Mount points or disk paths
    'disks' => [
        '/mnt/ftp', 
        '/mnt/ftp2'
    ],
    'default_disk_idx' => 0,

    // qBittorrent Settings
    'qb_url' => 'http://127.0.0.1:8080',
    'qb_user' => 'admin',
    'qb_pass' => 'xxxxxxx',
    
    // Action on torrent completion: 'stop', 'pause', 'remove'
    'action_on_complete' => 'stop',

    // Download categories/subdirectories
    // Key = qBittorrent category / subfolder
    // Value = Display Label in Telegram
    'categories' => [
        'tv' => '📺 Series',
        'movies' => '🎬 Movies',
        'download-pc' => '📁 Other',
        'Music' => '🎵 Music',
        'photo' => '🖼 Photo',
        'cartoni' => '🧸 Cartoni',
        'vidcontent' => '🎬 YouTube and others'
    ],

    // Timings (in seconds)
    'poll_timeout' => 10,
    'check_interval' => 30,
    'status_interval' => 86400,
    'state_save_interval' => 300,
    'notification_cleanup_time' => 120,

    // Status command display options
    'status_show_limit' => 10,      // Max torrents to show in /status (0 = unlimited)
    'status_filter' => 'all',       // 'all' = show all torrents, 'downloading' = only active downloads

    // TorrServer Settings
    'torrserver_enabled' => false,
    'torrserver_url' => 'http://127.0.0.1:8090',
    'torrserver_check_interval' => 60,
    'torrserver_user' => '', // Add if auth is enabled
    'torrserver_pass' => '', // Add if auth is enabled
    'torrserver_dl_limit_mbit' => 100, // Download speed limit in Mbit/s for TorrServer downloads

    // yt-dlp Settings
    // Ensure 'ytdlp_enabled' is true to use this feature.
    // By default, 'ytdlp_binary' relies on the system PATH. If yt-dlp is installed elsewhere
    // (e.g., ~/.local/bin/yt-dlp or /usr/local/bin/yt-dlp), provide the absolute path.
    // Additional parameters can be passed using 'ytdlp_extra_args'.
    'ytdlp_enabled' => false,
    'ytdlp_binary' => 'yt-dlp',
    'ytdlp_format' => 'bestvideo[height<=1080]+bestaudio/best[height<=1080]', // yt-dlp format selection
    'ytdlp_extra_args' => [],                      // Additional yt-dlp arguments, e.g. ['--cookies', '/path/to/cookies.txt']
    // URL domain allowlist for yt-dlp detection
    // Add or remove domains as needed. Must match the hostname part of the URL.
    'ytdlp_domains' => [
        'youtube.com',
        'youtu.be',
        'www.youtube.com',
        'm.youtube.com',
        'music.youtube.com',
        'vimeo.com',
        'twitter.com',
        'x.com',
        'instagram.com',
        'tiktok.com',
        'www.tiktok.com',
        'reddit.com',
        'www.reddit.com',
        'twitch.tv',
        'www.twitch.tv',
        'dailymotion.com',
        'www.dailymotion.com',
    ],

    // Jellyfin Settings (trigger library refresh after yt-dlp downloads)
    'jellyfin_enabled' => false,
    'jellyfin_url' => 'http://127.0.0.1:8096',
    'jellyfin_api_key' => '',  // Get from Jellyfin Dashboard → API Keys
];
